# Copy to .screenshots.env (gitignored via root *.env pattern) and fill in
# credentials for a DEDICATED MITx account used only for screenshots.
# The integration_test run will clear cookies on start and trigger a real
# sync, so don't point this at a personal account with curated lists.

SCREENSHOT_EMAIL=screenshots@example.com
SCREENSHOT_PASSWORD=replace-me
