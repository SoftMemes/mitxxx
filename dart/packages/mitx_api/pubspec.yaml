name: mitx_api
description: "Shared pure-Dart HTTP client for the MITx / MIT OpenLearning platform."
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  cookie_jar: ^4.0.8
  dio: ^5.8.0
  dio_cookie_manager: ^3.1.1
  logging: ^1.3.0
